[project]
name = "dify-datasource-webdav"
version = "0.0.1"
description = "Browse and ingest files from any WebDAV server into a Knowledge base"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32",
    "lxml>=5.0",]

[dependency-groups]
dev = []
